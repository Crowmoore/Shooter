#include "Logics.h"

Logics::Logics() {}
Logics::~Logics() {}

void Logics::handleEnemyCollision(vector <Enemy> enemies) {

	/*for (int i = 0; i < enemies.size(); i++) {
		for (int j = i + 1; j < enemies.size(); j++) {
			if (enemies[i].getGlobalBounds().intersects(enemies[j].getGlobalBounds())) {
				enemies[i].move((enemies[i].getSize().x / 16) - (enemies[j].getOrigin().x / 16), (enemies[i].getSize().y / 16) - (enemies[j].getOrigin().y / 16));
			}
		}
	}*/
}